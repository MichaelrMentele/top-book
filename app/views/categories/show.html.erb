<%= render 'shared/content_title.html.erb', title: @category.tag.capitalize %>

<%= render 'layouts/categories' %>

<div class="container">
  <ul class="list-inline text-center">
    <% @category.books.each do |book| %>
      <%= render 'books/book', book: book %>
    <% end %>
  </ul>
</div